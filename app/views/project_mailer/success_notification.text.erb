<%= @user.name %> 様

いつも「イラストクラウドファンディング」をご利用いただきありがとうございます。

この度は、プロジェクト「<%= @request.title %>」の目標達成、誠におめでとうございます！
本日、すべての支援者様への決済確定処理が完了いたしました。

■ プロジェクト最終ステータス
--------------------------------------------------
総支援額：¥<%= @request.total_amount.to_s(:delimited) %>
支援者数：<%= @request.support_histories.count %> 名
--------------------------------------------------

■ 今後の流れ
--------------------------------------------------
リクエストされた作品の制作に入っていただきます。
作品が完成したら、
--------------------------------------------------

<%= root_url %>